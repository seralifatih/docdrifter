{
  "name": "docdrifter-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply docdrifter-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply docdrifter-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.126.0",
    "typescript": "^5.7.0",
    "@cloudflare/workers-types": "^5.20260825.1"
  }
}
